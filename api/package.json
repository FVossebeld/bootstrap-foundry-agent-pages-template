{
  "name": "bootstrap-foundry-agent-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/{index.js,functions/*.js}",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@ag-ui/client": "^0.0.57",
    "@azure/ai-projects": "^2.0.0-beta.1",
    "@azure/functions": "^4.11.0",
    "@azure/identity": "^4.13.0",
    "@copilotkit/runtime": "^1.61.2"
  },
  "devDependencies": {
    "@types/node": "^22.19.3",
    "rimraf": "^5.0.10",
    "typescript": "^5.9.3"
  }
}
